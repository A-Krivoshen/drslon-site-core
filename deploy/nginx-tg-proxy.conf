# Preferred: on-domain proxy at /tg/ — see ../conf/nginx path:
#   /var/www/krivoshein.site/conf/nginx/telegram-proxy.conf
#
# Legacy subdomain tg.krivoshein.site is optional; main-domain /tg/ works without VPN in RU.